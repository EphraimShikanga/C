#include <stdio.h>

/**
 * main - Entry point
 *
 * Description:A program that prints _putchar, followed by a new line
 * Return: Always 0 (Success)
 */
int main(void)
{
	float a = 0;
	float b = 0.1;
	if (b>a)
	{
		printf("IS GREAT");
	}
	
	return (0);
}